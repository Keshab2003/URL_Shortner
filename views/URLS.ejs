<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URLS</title>
</head>
<body>
    <form action="url" method="post">
        <div>
        <label for="url" class="block text-gray-700 font-medium mb-1">Actual Link</label>
        <input type="url" name="url" id="url" class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
        <p class="text-sm text-gray-500 mt-1">Provide the link you want to get the shorter version for!</p>
      </div>
      <button type="submit" class="mt-6 w-full bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition">
      Generate
    </button>
    </form>

    <div class="next">
    <% urls.forEach( url =>{%>
        <li><%=url.shortUrl%></li>
    <%})%>
  </div>
    
</body>
</html>